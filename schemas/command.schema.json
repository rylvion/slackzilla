{
  "type": "object",

  "properties": {
    "$schema": {
      "type": "string"
    }
  },

  "patternProperties": {
    "^[a-z0-9-]+$": {
      "type": "object",
      "required": ["cmd", "description", "category", "file", "usage_hint"],
      "properties": {
        "cmd": {
          "type": "string",
          "pattern": "^/sz-[a-z0-9-]+$"
        },
        "description": {
          "type": "string",
          "minLength": 1,
          "maxLength": 60
        },
        "category": {
          "type": "string",
          "enum": [
            "core",
            "utility",
            "entertainment",
            "fun",
            "other"
          ]
        },
        "file": {
          "type": "string",
          "pattern": "^[a-z0-9-]+\\.js$"
        },
        "usage_hint": {
          "type": "string",
          "maxLength": 100
        }
      },
      "additionalProperties": false
    }
  },

  "additionalProperties": false
}